# Advanced Clevrr Computer - 100% Local AI Agent
# All dependencies run locally. No external APIs required.
#
# IMPORTANT: Ollama must be installed as a system tool, NOT via pip.
# Install Ollama from https://ollama.ai
# Then run:
#   ollama serve
#   ollama pull llava
#   ollama pull llama3
#
# Ollama provides the local LLM runtime for vision (llava) and
# text reasoning (llama3). It runs as a system service on localhost:11434.

# Computer Control
pyautogui>=0.9.54
pywinauto; sys_platform == "win32"
pygetwindow>=0.0.9; sys_platform == "win32"
mss>=9.0.1
opencv-python>=4.8.0
uiautomation; sys_platform == "win32"
pyperclip>=1.8.2

# Browser
playwright>=1.40.0
pytesseract>=0.3.10

# Vision & AI
Pillow>=10.0.0
numpy>=1.24.0
mediapipe>=0.10.0

# Voice (Local)
openai-whisper>=20231117
faster-whisper>=0.10.0
pyttsx3>=2.90
sounddevice>=0.4.6
webrtcvad>=2.0.10
openwakeword>=0.6.0
soundfile>=0.12.1
scipy>=1.11.0
RealtimeSTT>=0.2.0

# Memory & Semantic Search
sentence-transformers>=2.3.0

# LLM Communication
requests>=2.31.0
msgpack>=1.0.0

# Config & Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
psutil>=5.9.0

# Dashboard
gradio>=4.0.0
flask>=3.0.0

# LangChain / Agents
langchain>=0.3.0
langgraph>=0.4.0
langchain-openai>=0.6.0

# Auth
auth0-python>=4.7.0

# Testing
pytest>=7.0.0
pytest-timeout>=2.1.0
