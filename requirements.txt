# Advanced Clevrr Computer - 100% Local AI Agent
# All dependencies run locally. No external APIs required.
#
# IMPORTANT: Ollama must be installed as a system tool, NOT via pip.
# Install Ollama from https://ollama.ai
# Then run:
#   ollama serve
#   ollama pull llava
#   ollama pull llama3
#
# Ollama provides the local LLM runtime for vision (llava) and
# text reasoning (llama3). It runs as a system service on localhost:11434.

# --- Computer Control ---
pyautogui                          # Mouse + keyboard automation
pywinauto; sys_platform == "win32" # Semantic UI element finding (Windows only)
mss                                # Multi-monitor screen capture
opencv-python                      # Image processing and comparison
uiautomation; sys_platform == "win32"  # Windows UI Automation (Windows only)
pyperclip                          # Clipboard access for text pasting

# --- App Control ---
playwright                         # Browser automation (all platforms)
pytesseract                        # OCR support (optional, for advanced vision)

# --- Local AI (Voice) ---
openai-whisper                     # Offline speech-to-text (runs locally)
faster-whisper                     # Faster local transcription backend
pyttsx3                            # Offline text-to-speech (runs locally)
sounddevice                        # Microphone audio recording
webrtcvad                          # Voice activity detection
openwakeword                       # Wake-word detection backend
soundfile                          # Audio file handling
scipy                              # Signal processing utilities

# --- Local AI (Memory & Search) ---
sentence-transformers              # Local semantic search embeddings

# --- UI & Config ---
gradio                             # Local web dashboard (localhost:7860)
pyyaml                             # YAML config file parsing
Pillow                             # Image handling
numpy                              # Array/numerical processing
psutil                             # System health/process metrics
python-dotenv                      # Environment variable management

# --- HTTP (local only, for Ollama communication) ---
requests                           # HTTP client for local Ollama API
msgpack>=1.0
pytest>=7.0.0
pytest-timeout>=2.1.0
auth0-python>=4.7.0
python-dotenv>=1.0.0
requests>=2.31.0
flask>=3.0.0
langchain>=0.3.0
langgraph>=0.4.0
langchain-openai>=0.6.0
